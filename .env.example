# ========================================
# BARBERBOOK - Environment Variables
# ========================================
# 
# NAPOMENA: Ovo je samo TEMPLATE fajl za dokumentaciju.
# Pravi .env fajlovi su:
#   - backend/.env (za backend API)
#   - frontend/.env.local (za frontend React app)
#
# Docker automatski koristi vrednosti iz docker-compose.yml
# Ne treba ti ručno kreiranje .env fajlova!

# ========================================
# BACKEND (backend/.env)
# ========================================
# Database (PostgreSQL default credentials - rade na svakom računaru)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=barberbook_db
DB_SSLMODE=disable

# Email / SMTP
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=lazar.birtasevic1@gmail.com
MAIL_PASSWORD=dxwrtlynackrjgzb

# ========================================
# FRONTEND (frontend/.env.local)
# ========================================
VITE_API_BASE_URL=http://localhost:8080
VITE_APP_NAME=BarberBook
